import { assert } from "chai";

// Placeholder test so the anchor test layout is valid. The module agent that
// builds The Call replaces this with a real client test against the program.
describe("thecall", () => {
  it("has a valid test layout", () => {
    assert.isTrue(true);
  });
});
